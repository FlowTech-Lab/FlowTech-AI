networks:
  flowtech-net:
    driver: bridge

services:
  postgres:
    image: postgres:16-alpine
    hostname: postgres
    networks: ['flowtech-net']
    restart: unless-stopped
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - MAX_CONNECTIONS=100
      - SHARED_BUFFERS=256MB 
    volumes:
      - /opt/flowtech-ai/data/postgresql:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -h localhost -U ${POSTGRES_USER} -d ${POSTGRES_DB}']
      interval: 5s
      timeout: 5s
      retries: 10

  n8n:
    image: n8nio/n8n:latest
    hostname: n8n
    container_name: n8n
    restart: unless-stopped
    networks: ['flowtech-net']
    environment:
      - DB_TYPE=postgresdb
      - DB_POSTGRESDB_HOST=postgres
      - DB_POSTGRESDB_USER=${POSTGRES_USER}
      - DB_POSTGRESDB_PASSWORD=${POSTGRES_PASSWORD}
      - DB_POSTGRESDB_DATABASE=${POSTGRES_DB}
      - N8N_PUSH_BACKEND=sse
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_RUNNERS_ENABLED=false
      - N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
      - N8N_RUNNERS_ENABLED=true
      - N8N_PUSH_BACKEND=websocket
    ports:
      - "5678:5678"
    volumes:
      - /opt/flowtech-ai/data/n8n:/home/node/.n8n
      - /opt/flowtech-ai/data/n8n/backup:/backup
    depends_on:
      postgres:
        condition: service_healthy

  qdrant:
    image: qdrant/qdrant:latest
    hostname: qdrant
    container_name: qdrant
    networks: ['flowtech-net']
    restart: unless-stopped
    ports:
      - "6333:6333"
    volumes:
      - /opt/flowtech-ai/data/qdrant:/qdrant/storage

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: pgadmin
    restart: unless-stopped
    networks: ['flowtech-net']
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy
#  whisper:
#    image: onerahmet/openai-whisper-asr-webservice:latest
#    restart: unless-stopped
#    ports:
#      - "9000:9000"
#    environment:
#      - ASR_MODEL=small

#  piper:
#    image: rhasspy/wyoming-piper:latest
#    restart: unless-stopped
#    ports:
#      - "10200:10200"
#    environment:
#      - VOICE=fr_FR-siwis-low

#  comfyui:
#    image: ghcr.io/comfyanonymous/comfyui:latest
#    restart: unless-stopped
#    ports:
#      - "8188:8188"