---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.824863'
id: 6e0fc1f7-af00-4811-82ac-4e07403540ec
title: doc-6e0fc1f7-af00-4811-82ac-4e07403540ec
---

### 🧩 Structures & Nommage canoniques
- Entités: [[Module]], [[Service]], [[Component]], [[Config]].
- Données: id, name, status, created_at, updated_at.
- États: draft | active | archived | error.
- Fichiers génériques: `lib/app_arrays.rb`, `lib/app_hashes.rb`, `lib/utils.rb`.

### ✍️ Snippets essentiels
```rb
# Hello: dire bonjour avec prénom
def say_hello(first_name) puts "Bonjour, #{first_name} !" end

# IO: demander un prénom et le retourner
def ask_first_name
  print "Ton prénom ? "
  gets.strip
end

# Combo
def perform
  first_name = ask_first_name
  say_hello(first_name)
end
```

```rb
# Pyramide simple (moitié)
def half_pyramid(height)
  1.upto(height) { |i| puts ("#" * (2*i-1)).rjust(height + i - 1) }
end
```

```rb
# Pyramide pleine (Gizeh)
def full_pyramid(height)
  puts "Voici la pyramide :"
  1.upto(height) { |i| puts ("#" * (2*i-1)).center(2*height-1) }
end
```