---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.820896'
id: 5557d2ef-5736-4317-b860-973025609197
title: doc-5557d2ef-5736-4317-b860-973025609197
---

```rb
# Objectif: exécuter une commande shell
system("pwd")
```

### 🔗 Relations / Composition
- Script `GoogleSearcher` dépend de Module [[Component]] URL builder et de [[Service]] navigateur.  
- Script `mkdiruby` compose: `Dir`, `File`, `system`, et [[Config]] Gemfile/RSPEC.  
- Aliases relient le shell à ces Scripts.

### 🧪 Test / Validation
```bash
# Lancer rubocop ou un linter rapide si présent
ruby -c google_searcher.rb
ruby -c mkdiruby.rb
rspec --init >/dev/null 2>&1 || true
```

### ⚙️ Build / Run / Deploy
- Exécution directe par `ruby script.rb`.  
- Distribution: placer dans `~/tools/` et référencer via alias.  
- Pas de build; dépendances via Gemfile + `bundle install` si projet.

### 🚫 Pièges & bonnes pratiques
- Vérifier `ARGV` vide et afficher un `Usage:` clair.  
- Utiliser `URI.encode_www_form_component` pour les requêtes.  
- Toujours `close` ou `File.write` direct pour éviter fuites.  
- Ne pas surécrire des fichiers critiques sans sauvegarde.