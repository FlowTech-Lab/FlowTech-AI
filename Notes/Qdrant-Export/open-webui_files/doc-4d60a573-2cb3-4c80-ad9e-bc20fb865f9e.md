---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.820038'
id: 4d60a573-2cb3-4c80-ad9e-bc20fb865f9e
title: doc-4d60a573-2cb3-4c80-ad9e-bc20fb865f9e
---

# Afficher la clé publique
cat ~/.ssh/id_ed25519.pub

# Démarrer l'agent et ajouter la clé (session)
eval "$(ssh-agent -s)"; ssh-add ~/.ssh/id_ed25519

# Tester l'accès GitHub
ssh -T git@github.com

# Config Git globale
git config --global user.name "GITHUB_USERNAME"
git config --global user.email "EMAIL"

# Cloner en SSH
git clone git@github.com:ORG_OR_USER/REPO.git

# Convertir un remote HTTPS -> SSH
git remote set-url origin git@github.com:ORG_OR_USER/REPO.git
```

### 🧩 Structures & Nommage canoniques
- **Entities**: Keypair, Agent, KnownHosts, Remote, Repo, Config.
- **Config files**: `~/.ssh/id_ed25519`, `~/.ssh/id_ed25519.pub`, `~/.ssh/config`, `~/.ssh/known_hosts`, `~/.gitconfig`.
- **Données génériques**: id, name, email, host, created_at, updated_at.
- **États**: draft | active | archived | error.
- **Scripts génériques**: `lib/utils.rb`, `script_A.sh`, `app_arrays.rb` (adapter).