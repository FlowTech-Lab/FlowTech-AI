---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.820543'
id: 52c24cf6-b456-424c-956f-c962f19d8f41
title: doc-52c24cf6-b456-424c-956f-c962f19d8f41
---

### 🔗 Relations / Composition
- [[Service]] `MarketData` fournit `currencies` et `prices` à [[Module]] `Analytics`.
- `Analytics` consomme [[Collection]] `items`, produit un [[Hash]] `sizes`.
- [[Component]] `Reporter` lit `sizes`, `cheapest`, génère sortie CLI.

### 🧪 Test / Validation
```bash
# Init RSpec
rspec --init
# Sanity check express
ruby -e 'raise "fail" unless %w[a b].size == 2'
```

### ⚙️ Build / Run / Deploy
- Local: `ruby script_A.rb` puis exporter artefacts.
```rb
require "json"; require "csv"
File.write("report.json", {sizes: sizes}.to_json)
CSV.open("report.csv","w"){|csv| sizes.each{|k,v| csv << [k,v]}}
```
- CI simple: `rubocop && rspec`.

### 🚫 Pièges & bonnes pratiques
- Remplacer toute donnée réelle par `INPUT` ou exemples synthétiques.
- Préférer `sort_by/min_by/max_by` aux boucles manuelles.
- Normaliser avant agrégation: `strip`, `downcase`, `gsub`.
- Après `min/max`, gérer ex æquo avec `select`.
- Vérifier tailles avant `zip` pour éviter pertes d'éléments.