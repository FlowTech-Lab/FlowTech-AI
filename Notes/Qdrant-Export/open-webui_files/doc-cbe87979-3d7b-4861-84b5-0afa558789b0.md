---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.841183'
id: cbe87979-3d7b-4861-84b5-0afa558789b0
title: doc-cbe87979-3d7b-4861-84b5-0afa558789b0
---

### 🔗 Relations / Composition
- 1-N: `User -> Article` via `articles.user_id`.
- N-N: `Article <-> Category` via `article_categories`.
- N-N: `Category <-> Tag` via `category_tags`.
- Hiérarchie de commentaires: utiliser `comments(parent_id NULLABLE, link_type, link_id)` ou `comments(id, parent_id)` pour profondeur 1.

### 🧪 Test / Validation
```bash
# Activer l'intégrité FK et vérifier des comptages
sqlite3 db.sqlite "PRAGMA foreign_keys = ON;"
sqlite3 db.sqlite "SELECT COUNT(*) FROM users;"
sqlite3 db.sqlite "EXPLAIN QUERY PLAN SELECT * FROM articles WHERE user_id=1;"
```

### ⚙️ Build / Run / Deploy
- Créer le schéma: `sqlite3 db.sqlite < schema.sql`.
- Insérer des exemples synthétiques: `sqlite3 db.sqlite < seed.sql`.
- Sauvegarder: copier `db.sqlite` ou exporter `.dump`.
```bash
sqlite3 db.sqlite ".backup db_backup.sqlite"
sqlite3 db.sqlite ".dump" > full_dump.sql
```