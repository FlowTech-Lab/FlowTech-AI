---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.832253'
id: b075cd20-370d-4a26-8f95-d05cde92af4e
title: doc-b075cd20-370d-4a26-8f95-d05cde92af4e
---

```rb
# Objectif: ouvrir l'URL avec Launchy
require "launchy"
Launchy.open(url)
```

```rb
# Objectif: créer un squelette d'app Ruby
app = ARGV.first or abort("Usage: ruby mkdiruby.rb APP_NAME")
Dir.mkdir(app)
Dir.chdir(app) do
  File.write("Gemfile", "source 'https://rubygems.org'\n\ngem 'rspec'\ngem 'pry'\n")
  system("git init")
  File.write(".env", "# ENV=values\n")
  File.write(".gitignore", ".env\n")
  Dir.mkdir("lib")
  system("rspec --init")
  File.write("README.md", "# App Ruby\n")
end
puts "OK: {app} créé."
```

```rb
# Objectif: écrire/append/lire un fichier
f = File.open("app.log", "a"); f.puts("created_at=#{Time.now}"); f.close
puts File.read("Gemfile")
```

```rb
# Objectif: exécuter une commande shell
system("pwd")
```