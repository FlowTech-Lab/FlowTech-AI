---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.830985'
id: a724cb09-bd60-41db-9602-532a3e51c300
title: doc-a724cb09-bd60-41db-9602-532a3e51c300
---

### ✍️ Snippets essentiels
```bash
# Objectif: créer et pousser un dépôt propre
git init && git add .
git commit -m "feat: initial commit"
git branch -M main
git remote add origin SSH_OR_HTTPS_URL
git push -u origin main
```
```bash
# Objectif: revenir à un état stable sans garder le travail en cours
git reset --hard HEAD
git clean -fd  # supprime fichiers non suivis (attention)
```
```bash
# Objectif: stationner un travail non prêt puis le reprendre
git stash push -m "WIP: FEATURE_X"
git switch main
# ... plus tard
git switch -
git stash pop
```
```bash
# Objectif: inspecter précisément ce qui partira dans le prochain commit
git diff --staged
```
```bash
# Objectif: configurer SSH et tester la connexion
ssh-keygen -t ed25519 -C "EMAIL"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
ssh -T git@github.com
```