---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.822289'
id: 61b3b066-35a2-4a72-96b6-f8aa932e2741
title: doc-61b3b066-35a2-4a72-96b6-f8aa932e2741
---

### 🧩 Structures & Nommage canoniques
- Données: `Array`, `Integer`, `String`, `Boolean`.
- Index: commence à `0`.
- Fichiers: `lib/app_arrays.rb`, `lib/utils.rb`, `spec/app_arrays_spec.rb`.
- États génériques: `draft | active | archived | error`.
- Attributs usuels: `id, name, status, created_at, updated_at`.

### ✍️ Snippets essentiels
```rb
# Créer un array et accéder par index
items = ["A", "B", "C"]
items[0]          # => "A"
items.length      # => 3
```

```rb
# Modifier une case
items[1] = "X"    # ["A","X","C"]
```

```rb
# Boucle simple: n.times
3.times do
  puts "hello"
end
```

```rb
# Boucle for .. in .. avec compteur
for count in (1..3)
  puts count
end
```

```rb
# Parcourir un array: each
prices = [10, 20, 30]
prices.each do |price|
  puts price
end
```

```rb
# Transformer un array: map (non destructif) / map!
doubles = prices.map { |p| p * 2 } # => [20,40,60]
prices.map! { |p| p + 1 }          # prices devient [11,21,31]
```