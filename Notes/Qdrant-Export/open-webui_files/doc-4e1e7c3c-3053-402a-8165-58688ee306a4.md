---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.820268'
id: 4e1e7c3c-3053-402a-8165-58688ee306a4
title: doc-4e1e7c3c-3053-402a-8165-58688ee306a4
---

### 🧩 Structures & Nommage canoniques
- Entités: [[Entity]], [[Item]], [[Collection]], [[Service]], [[Module]], [[Component]], [[Config]]
- Données: `id`, `name`, `status`, `created_at`, `updated_at`
- États: `draft` | `active` | `archived` | `error`
- Fichiers génériques: `lib/app_arrays.rb`, `lib/app_hashes.rb`, `lib/utils.rb`

### ✍️ Snippets essentiels
```rb
# Boucle simple: compter jusqu'à N
N = 5
1.upto(N) { |i| puts i }

# Lire/écrire des données basiques
items = ["A", "B", "C"]
items.each { |x| puts "Item: #{x}" }

# Méthode pure avec garde
def normalize(input)
  return "" if input.nil?
  input.to_s.strip.downcase
end

# Classe minimale avec attributs
class Entity
  attr_accessor :id, :name, :status
  def initialize(id:, name:, status: "draft")
    @id, @name, @status = id, name, status
  end
end
```