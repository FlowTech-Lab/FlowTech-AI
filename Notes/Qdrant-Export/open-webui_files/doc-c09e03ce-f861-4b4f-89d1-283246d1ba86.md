---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.839864'
id: c09e03ce-f861-4b4f-89d1-283246d1ba86
title: doc-c09e03ce-f861-4b4f-89d1-283246d1ba86
---

### ✍️ Snippets essentiels
```rb
# Validation de présence et d'unicité d'un champ
class User < ApplicationRecord
  validates :email, presence: true, uniqueness: true
end
```
```rb
# Validation de format simple (placeholder EMAIL)
class User < ApplicationRecord
  validates :email, format: { with: /\A[^@\s]+@([^@\s]+\.)+[^@\s]+\z/,
                              message: "format email requis" }
end
```
```rb
# Longueur minimale/maximale et plage
class Entity < ApplicationRecord
  validates :name, length: { minimum: 2 }
  validates :status, length: { maximum: 20 }
end
```
```rb
# Méthode d'instance calculée (ex: fin d'événement)
class Session < ApplicationRecord
  belongs_to :course
  def end_date
    start_date + course.weeks.to_i.week
  end
end
```
```rb
# Callback après création (ex: e-mail de bienvenue)
class User < ApplicationRecord
  after_create :send_welcome_email
  def send_welcome_email
    # Mailer.generic_welcome(self).deliver_later
  end
end
```