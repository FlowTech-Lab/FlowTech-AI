---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.827596'
id: 849f051c-77c4-4da2-b957-cdf196c61cb8
title: doc-849f051c-77c4-4da2-b957-cdf196c61cb8
---

```rb
# Migration: clés étrangères et références
class AddAuthorToPosts < ActiveRecord::Migration[7.1]
  def change
    add_reference :posts, :author, null: false, foreign_key: { to_table: :users }
  end
end
```

```rb
# seeds: données d'exemple idempotentes
emails = %w[user1@INPUT user2@INPUT user3@INPUT]
emails.each do |e|
  User.find_or_create_by!(email: e) do |u|
    u.first_name = "Name"
    u.last_name  = "Item"
  end
end
```

```rb
# Console: CRUD minimal
u = User.create(email: "INPUT@INPUT")
u.update(first_name: "INPUT")
u.destroy
```

### 🔗 Relations / Composition
- [[Association]]s classiques:
  - `User` **has_many** `Post`
  - `Post` **belongs_to** `User` (colonne `user_id` + FK)
  - N‑N via [[Join Table]]: `has_many :through` avec `Membership`.
- Importer modules de [[Service]] ou [[Component]] depuis `app/` ou `lib/`.
- Les migrations décrivent la structure. Les models décrivent comportements, validations, et liens.