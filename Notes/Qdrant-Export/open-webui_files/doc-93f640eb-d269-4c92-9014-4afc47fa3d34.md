---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.829068'
id: 93f640eb-d269-4c92-9014-4afc47fa3d34
title: doc-93f640eb-d269-4c92-9014-4afc47fa3d34
---

class Doctor < ApplicationRecord
  has_many :appointments
  has_many :patients, through: :appointments
end

class Patient < ApplicationRecord
  has_many :appointments
  has_many :doctors, through: :appointments
end
```
```rb
# Migration: ajouter une référence avec index et FK
class AddUserRefToArticles < ActiveRecord::Migration[7.2]
  def change
    add_reference :articles, :user, null: false, foreign_key: true, index: true
  end
end
```

### 🔗 Relations / Composition
- 1‑N: `Article` contient `user_id` → navigation: `article.user` et `user.articles`.
- N‑N: `Doctor` ↔ `Patient` via `Appointment` → `doctor.patients`, `patient.doctors`.
- Préférer `has_many :through` à `has_and_belongs_to_many` pour stocker des métadonnées du lien.

### 🧪 Test / Validation
```bash
# en console
u = User.create(first_name:"A", last_name:"B", city:"X", age:1)
a = Article.create(title:"T", subject:"S", content:"INPUT", user:u)
a.user        # => User
u.articles    # => [Article, ...]