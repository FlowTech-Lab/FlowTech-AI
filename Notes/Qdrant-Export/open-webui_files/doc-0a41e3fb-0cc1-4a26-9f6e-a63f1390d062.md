---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.812888'
id: 0a41e3fb-0cc1-4a26-9f6e-a63f1390d062
title: doc-0a41e3fb-0cc1-4a26-9f6e-a63f1390d062
---

### 🧪 Test / Validation
```bash
# Initialiser
rspec --init

# Exemple de spec minimal
# spec/multiples_spec.rb
describe "#is_multiple_of_3_or_5?" do
  it "true on 3 or 5 multiples" do
    expect(is_multiple_of_3_or_5?(3)).to eq(true)
    expect(is_multiple_of_3_or_5?(5)).to eq(true)
  end
  it "false otherwise" do
    expect(is_multiple_of_3_or_5?(2)).to eq(false)
  end
end
```

### ⚙️ Build / Run / Deploy
- Local: `ruby lib/script_A.rb` ou via `rake` si ajouté.
- CI simple: exécuter `bundle install && rubocop && rspec`.
- Packaging: publier en gem plus tard si réutilisable ([[Module]] → [[Service]]).

### 🚫 Pièges & bonnes pratiques
- Ne jamais vendre avant d'acheter dans `day_trader`.
- Toujours valider types d'INPUT et gérer erreurs propres.
- Préférer itérations pures et tests unitaires isolés.
- Respecter casse et rebouclage `z→a` dans César.
- Ne pas coller de listes réelles dans les fichiers du repo.