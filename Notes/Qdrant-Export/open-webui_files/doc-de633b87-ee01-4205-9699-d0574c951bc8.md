---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.844036'
id: de633b87-ee01-4205-9699-d0574c951bc8
title: doc-de633b87-ee01-4205-9699-d0574c951bc8
---

### 📦 Commandes clés
```bash
irb
ruby script_A.rb
bundle exec rspec
```

### 🧩 Structures & Nommage canoniques
- Méthodes en `snake_case` : `ask_input`, `compute_total`.
- 1 méthode = 1 rôle. 25 lignes max.
- Orchestrateur : `perform` appelle des sous-méthodes.
- Données génériques : id, name, status, created_at, updated_at.
- États : draft | active | archived | error.
- Fichiers génériques : `lib/utils.rb`, `lib/app_arrays.rb`, `lib/app_hashes.rb`.

### ✍️ Snippets essentiels
```rb
# Définition + retour explicite
def greet(name)
  return "Bonjour, #{name}"
end
```

```rb
# Arguments avec valeur par défaut
def price_with_tax(price, rate = 0.2)
  price * (1 + rate)
end
```

```rb
# Arguments nommés
def connect(host:, port:, secure: true)
  { host: host, port: port, secure: secure }
end
```

```rb
# Splat / rest et double splat
def sum(*nums) ; nums.sum ; end
def build(**opts) ; opts ; end
```