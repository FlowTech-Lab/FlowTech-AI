---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.840243'
id: c3747293-802b-4212-9450-bd9ea3e5464d
title: doc-c3747293-802b-4212-9450-bd9ea3e5464d
---

RSpec.describe "hello" do
  it "returns greeting" do
    expect(hello).to eq("Hello world!")
  end
end
```
```rb
# Objectif: valider des entrées (INPUT borné 1..25)
def build_pyramid(levels)
  raise ArgumentError, "levels must be 1..25" unless (1..25).include?(levels)
  Array.new(levels) { |i| ("#" * (2*i+1)).center(2*levels-1) }.join("\n")
end
```
```rb
# Objectif: test des erreurs attendues
RSpec.describe "#build_pyramid" do
  it "rejects invalid INPUT" do
    expect { build_pyramid(0) }.to raise_error(ArgumentError)
  end
end
```
```rb
# Objectif: exemple de stub/mocks
class Service
  def initialize(api) = @api = api
  def fetch(id) = @api.get(id) # dépendance externe
end

RSpec.describe Service do
  it "uses API dependency" do
    api = double("Api", get: { id: 1, name: "Item" })
    expect(Service.new(api).fetch(1)).to include(:id, :name)
  end
end
```