---
collection: open-webui_files
exported_at: '2026-01-11T20:52:33.842937'
id: d7eaa7d5-e16f-4310-8e07-9f191eeafb36
title: doc-d7eaa7d5-e16f-4310-8e07-9f191eeafb36
---

### ✍️ Snippets essentiels
```rb
# Objectif: créer N items de test avec des placeholders
N.times do |i|
  User.create(first_name: "Name{i}", email: "user{i}@example.test", status: "active")
end
```
```rb
# Objectif: lecture ciblée et tri
User.where(status: "active").order(created_at: :desc).limit(10)
```
```rb
# Objectif: mise à jour en masse sécurisée
User.where(status: "draft").update_all(status: "active")
```
```rb
# Objectif: affichage tabulaire lisible (si table_print est installé)
tp User.select(:id, :first_name, :email).limit(5)
```
```rb
# Objectif: sandbox pour essais sûrs (à lancer côté CLI)
# rails c --sandbox
```

### 🔗 Relations / Composition
- Un [[Model]] appartient à une [[Collection]] en BDD via ActiveRecord.
- Les requêtes de classe (`User.where`) composent des scopes chaînables.
- Les objets d'instance (`user.update`) affectent une seule entrée.
- Le chargeur `reload!` recharge les classes pour tester sans relancer la console.