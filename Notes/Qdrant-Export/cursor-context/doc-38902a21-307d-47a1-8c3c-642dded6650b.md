---
collection: cursor-context
exported_at: '2026-01-11T22:30:22.005632'
id: 38902a21-307d-47a1-8c3c-642dded6650b
title: doc-38902a21-307d-47a1-8c3c-642dded6650b
---

Grenoble Roller Project - Events Controller N+1 Query Fixes:

EVENTSCONTROLLER#SHOW:
Current: @event = Event.includes(:route, :creator_user).find(params[:id])
Fix if attendances.users displayed: @event = Event.includes(:route, :creator_user, attendances: :user).find(params[:id])

ATTENDANCESCONTROLLER#INDEX:
Current: @attendances = current_user.attendances.active.includes(event: :route).order('events.start_at ASC')
Fix: @attendances = current_user.attendances.active.includes(event: [:route, :creator_user]).order('events.start_at ASC')
Reason: event.creator_user is used in _event_card.html.erb partial

PAGESCONTROLLER#INDEX:
Current: @highlighted_event = Event.published.upcoming.includes(:route, :creator_user, :attendances).order(:start_at).first
Fix if attendances.users displayed: @highlighted_event = Event.published.upcoming.includes(:route, :creator_user, attendances: :user).order(:start_at).first

BULLET AUDIT PROCESS:
1. Run bundle install in Docker
2. Start server: docker compose -f ops/dev/docker-compose.yml up web
3. Visit pages: homepage, events index, events show, attendances index
4. Check Bullet alerts in browser console, HTML footer, Rails logs, bullet.log
5. Fix N+1 queries by adding missing includes
6. Verify Bullet no longer reports errors
7. Run RSpec tests to ensure they still pass